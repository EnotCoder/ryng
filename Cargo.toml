[package]
name = "app"
version = "0.1.0"
edition = "2024"

[lib]
name = "app"
crate-type = ["lib", "cdylib"]



[dependencies]
bevy = { version = "0.19.1", features = ["android-game-activity", "mp3"] }

[profile.release]
lto = "thin"
strip = true
